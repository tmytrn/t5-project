import React, { useRef } from "react";
import Head from "next/head";
import Image from "next/image";
import Link from "next/link";
import period from "../admin/schemas/period";
import { getAllDiscs, getAllPeriods } from "../data";
import styles from "../styles/Home.module.scss";
import MyLayout from "components/Layout";
import { motion } from "framer-motion";
import Slider from "react-slick";

const settings = {
  arrows: false,
  dots: false,
  infinite: false,
  slidesToShow: 7,
  slidesToScroll: 1,
  vertical: true,
  verticalSwiping: true,
  centerMode: true,
  swipeToSlide: true,
  afterChange: (index) => {
    console.log("Slide: ", index);
  },
};

const Home = ({ periods }) => {
  const ref = useRef();
  return (
    <div className="bg-sunset">
      <Head>
        <title>A Murmuration</title>
        <meta name="description" content="Generated by create next app" />
        {/* <link rel="icon" href="/favicon.ico" /> */}
      </Head>
      <main
        className="pt-[310px] px-5 border-solid border-saddle h-[calc(100vh - 310px)] w-full bg-transparent overflow-hidden"
        ref={ref}
      >
        <Slider {...settings}>
          {periods.map((period, key) => (
            <div key={key} className="flex items-center snap-center">
              <Link href={`/period/${period.period}`}>
                <a className="inline-block text-7xl pl-1 tracking-tightest text-saddle text-center">
                  {period.period.split("-")[0]} {period.period.split("-")[1]}
                </a>
              </Link>
            </div>
          ))}
        </Slider>
      </main>
    </div>
  );
};

export async function getStaticProps() {
  const periods = await getAllPeriods();
  const page = "home";
  return {
    props: { periods, page },
    revalidate: 10,
  };
}

Home.Layout = MyLayout;
export default Home;
